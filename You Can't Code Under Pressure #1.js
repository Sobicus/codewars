//Code as fast as you can! You need to double the integer and return it.

function doubleInteger(i) {
    // i will be an integer. Double it and return it.
    return i * 2;
}

function outed(meet, boss) {
    let acc = 0
    let announcement = ''
    let arrMeet = Object.values(test)
    for (let i = 0; arrMeet.length > i; i++) {
        acc += arrMeet[i]
    }
    if (meet[boss]) {
        return acc + meet[boss]
    }
    if ((acc) / (arrMeet.length + 1) <= 5) {
        return announcement = 'Get Out Now!'
    } else {
        return announcement = 'Nice Work Champ!'
    }
    return announcement
}